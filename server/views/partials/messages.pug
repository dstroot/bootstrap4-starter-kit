messages
  if messages
    if messages.error
      .alert.alert-danger.alert-dismissible.fade.show.rounded-0.mb-0(role='alert')
        for error in messages.error
          p.lead.mb-0
            i.fas.fa-exclamation-triangle
            | &nbsp;&nbsp;#{error}
            button.close(type='button', data-dismiss='alert', aria-label='Close')
              span(aria-hidden='true') &times;
    if messages.warning
      .alert.alert-warning.alert-dismissible.fade.show.rounded-0.mb-0(role='alert')
        for warning in messages.warning
          p.lead.mb-0
            i.fas.fa-exclamation-triangle
            | &nbsp;&nbsp;#{warning}
            button.close(type='button', data-dismiss='alert', aria-label='Close')
              span(aria-hidden='true') &times;
    if messages.info
      .alert.alert-info.alert-dismissible.fade.show.rounded-0.mb-0(role='alert')
        for info in messages.info
          p.lead.mb-0
            i.fas.fa-info-circle
            | &nbsp;&nbsp;#{info}
            button.close(type='button', data-dismiss='alert', aria-label='Close')
              span(aria-hidden='true') &times;
    if messages.success
      .alert.alert-success.alert-dismissible.fade.show.rounded-0.mb-0(role='alert')
        for success in messages.success
          p.lead.mb-0
            i.fas.fa-check-circle
            | &nbsp;&nbsp;#{success}
            button.close(type='button', data-dismiss='alert', aria-label='Close')
              span(aria-hidden='true') &times;
